import React from "react";

const BackButton = (props) => {
  return (
    <React.Fragment>
      <a href='../'>
        <p>← Go back</p>
      </a>
    </React.Fragment>
  );
};

export default BackButton;
